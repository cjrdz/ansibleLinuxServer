---
# Inventory - Host-specific variables are in host_vars/
all:
  children:
    ubuntu_servers:
      hosts:
        serverMaster:
        serverNode1:
      vars:
        ansible_python_interpreter: /usr/bin/python3
        ansible_user: ubuntu
